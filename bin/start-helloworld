#!/bin/bash

demo_dir="$( cd "$(dirname "$0")/.." ; pwd -P )"

export CONJUR_AUTHN_API_KEY=$(\
    docker-compose --project-directory "$demo_dir" \
    run -T conjur-cli host rotate_api_key --host helloworld \
    | tr -d "\r\n")

docker-compose --project-directory "$demo_dir" \
    up -d --no-recreate helloworld